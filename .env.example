# Copy to `.env` and adjust if needed. Sensible defaults work out of the box
# for a localhost demo — no external API keys required.

DATABASE_URL="file:./prisma/dev.db"

# Where the local FastAPI ML server is reachable. Only change if you moved it.
ML_SERVICE_URL="http://localhost:8000"
